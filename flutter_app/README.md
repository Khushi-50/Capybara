# CodeQuest Flutter

Manual Flutter codebase matching the React frontend flow:

- login / signup
- home path map
- leaderboard
- today
- profile
- quiz flow

## Before running

1. Install Flutter SDK.
2. Update `lib/config/app_config.dart` with your backend LAN URL.
3. Run:

```bash
flutter pub get
flutter run
```
